# Aperture Methods compatibility adapter

Python implementation for standard local safetensors inspection and Transformers/Accelerate dispatch. This code is included for compatibility. Native GPU execution is qualified separately from the Node controller; see the repository-level VERIFICATION.md. No model weights or external native binaries are distributed here.
